services:
  - type: web
    name: quiz-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
